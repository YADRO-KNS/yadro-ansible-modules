---
- name: Test BMC reset to defaults | Restart BMC
  yadro.obmc.bmc_reset_to_defaults:
    connection: "{{ connection }}"
    reset_type: all
  register: result

- name: Test BMC reset to defaults | Check state
  assert:
    that: "result.changed == true"